import { PencilLine, Trash2 } from 'lucide-react'
import { formatMoney } from '../../utils/formatters.js'
export default function AccountList({ accounts, onEdit, onDelete }) { return <div className="account-list">{accounts.map(account => <article className="account-row" key={account.id}><span className="account-icon" style={{ background: account.color }} aria-hidden="true">{account.icon}</span><div><b>{account.name}</b><span>{account.type} · {account.currency}{!account.is_active && ' · Inactive'}</span></div><strong className={Number(account.current_balance) < 0 ? 'negative' : ''}>{formatMoney(account.current_balance, account.currency)}</strong><div className="row-actions"><button aria-label={`Edit ${account.name}`} onClick={() => onEdit(account)}><PencilLine size={16} />Edit</button><button aria-label={`Delete ${account.name}`} onClick={() => onDelete(account)}><Trash2 size={16} />Delete</button></div></article>)}</div> }
